import React from "react";
import { View, Text } from "@react-pdf/renderer";

const Header = () => {
  return (
    <View>
      <Text>dari Component Header</Text>
    </View>
  );
};

export default Header;
